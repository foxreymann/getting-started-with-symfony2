sensio_todo:
    resource: "@SensioTodoBundle/Controller/"
    type:     annotation
    prefix:   /todo

sensio_training:
    resource: "@SensioTrainingBundle/Controller/"
    type:     annotation
    prefix:   /training

